<%= form_for(current_user.invites.find_by(attended_event_id: @event.id), remote: true) do |f| %>
  <%= f.hidden_field :attended_event_id, :value => @event.id %>
  <%= f.submit "Attend Event", class: "form-control btn btn-success" %>
<% end %>
