<%= form_for(current_user.invites.find_by(attended_event_id: @event.id), remote: true, html: { method: :delete }) do |f| %>
  <%= f.submit "Unattend event", class: "form-control btn btn-danger" %>
<% end %>
